export enum ActionTypes {
    SetUser = '[USER] set user details',
    LoadTraining = '[Training] Load items from server',
    Logout = '[App] logout',
    OpenTraining = '[Training] Open Training'
}
